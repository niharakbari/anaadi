# AI Models

This directory stores runtime AI model assets.

Contents are intentionally excluded from Git because model files can be several hundred megabytes.

Current model:

- OpenCLIP ViT-B-32
  - visual.onnx

Developers should download the required model separately before running the backend.